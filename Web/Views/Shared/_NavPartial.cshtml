<nav class="layout-navbar container-xxl navbar navbar-expand-xl navbar-detached align-items-center bg-navbar-theme flex-shrink-0" id="layout-navbar">
    <div class="layout-menu-toggle navbar-nav align-items-xl-center me-3 me-xl-0 d-xl-none">
        <a class="nav-item nav-link px-0 me-xl-4" href="javascript:void(0)">
            <i class="bx bx-menu bx-sm"></i>
        </a>
    </div>

    <div class="navbar-nav-right d-flex align-items-center" id="navbar-collapse">
        <div class="navbar-nav align-items-center">
            <div class="nav-item d-flex align-items-center">
                <i class="bx bx-search fs-4 lh-0"></i>
                <input type="text"
                       class="form-control border-0 shadow-none"
                       placeholder="Search..."
                       aria-label="Search..." />
            </div>
        </div>
        <ul class="navbar-nav flex-row align-items-center ms-auto">
            <li class="nav-item lh-1 me-3">
                <div class="d-flex align-items-center">
                    <i class="bx bx-user-circle me-2 text-primary" style="font-size: 20px;"></i>
                    <span class="fw-bold text-dark">
                        @(ViewBag.UserName ?? "Kullanıcı")
                    </span>
                </div>
            </li>

            <li class="nav-item navbar-dropdown dropdown-user dropdown">
                <a class="nav-link dropdown-toggle hide-arrow" href="javascript:void(0);" data-bs-toggle="dropdown">
                    <div class="avatar avatar-online">
                        <img src="@(string.IsNullOrEmpty(ViewBag.ProfileImage) ? "/assets/img/avatars/1.png" : ViewBag.ProfileImage)"
                             alt="Profile"
                             class="w-px-40 h-auto rounded-circle"
                             onerror="this.src='/assets/img/avatars/1.png';" />
                    </div>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                    <li>
                        <a class="dropdown-item" href="@Url.Action("Index", "Manage")">
                            <div class="d-flex">
                                <div class="flex-shrink-0 me-3">
                                    <div class="avatar avatar-online">
                                        <img src="@(string.IsNullOrEmpty(ViewBag.ProfileImage) ? "/assets/img/avatars/1.png" : ViewBag.ProfileImage)"
                                             alt="Profile"
                                             class="w-px-40 h-auto rounded-circle"
                                             onerror="this.src='/assets/img/avatars/1.png';" />
                                    </div>
                                </div>
                                <div class="flex-grow-1">
                                    <span class="fw-semibold d-block">@ViewBag.UserName</span>
                                    <small class="text-muted">Kullanıcı</small>
                                </div>
                            </div>
                        </a>
                    </li>
                    <li>
                        <div class="dropdown-divider"></div>
                    </li>
                    <li>
                        <a class="dropdown-item" href="@Url.Action("Index", "Manage")">
                            <i class="bx bx-user me-2"></i>
                            <span class="align-middle">Profilim</span>
                        </a>
                    </li>
                    <li>
                        <a class="dropdown-item" href="#">
                            <i class="bx bx-cog me-2"></i>
                            <span class="align-middle">Ayarlar</span>
                        </a>
                    </li>
                    <li>
                        <div class="dropdown-divider"></div>
                    </li>
                    <li>
                        @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
                        {
                            @Html.AntiForgeryToken()
                            <button type="submit" class="dropdown-item">
                                <i class="bx bx-power-off me-2"></i>
                                <span class="align-middle">Çıkış Yap</span>
                            </button>
                        }
                    </li>
                </ul>
            </li>
        </ul>
    </div>
</nav>